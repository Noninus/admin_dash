import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:admin_dash/app/modules/loading/loading_module.dart';

void main() {
  initModule(LoadingModule());
  // LoadingController loading;
  //
  setUp(() {
    //     loading = LoadingModule.to.get<LoadingController>();
  });

  group('LoadingController Test', () {
    //   test("First Test", () {
    //     expect(loading, isInstanceOf<LoadingController>());
    //   });

    //   test("Set Value", () {
    //     expect(loading.value, equals(0));
    //     loading.increment();
    //     expect(loading.value, equals(1));
    //   });
  });
}
